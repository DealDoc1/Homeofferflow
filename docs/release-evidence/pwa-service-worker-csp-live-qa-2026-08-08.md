# PWA service-worker CSP live QA — 2026-08-08

## Change

Production commit `0c66ef3` adds `'self'` to the `worker-src` Content-Security-
Policy directive. The previous production policy allowed only `blob:` workers,
which blocked the same-origin `/service-worker.js` registration.

## Verification

- Production deployment: `dpl_7wuuzb9coSUrK1CX1VMfXMAnygM1`
- Canonical URL: `https://www.homeofferflow.com/`
- Live response status: `200`
- Live CSP includes: `worker-src 'self' blob:`
- Live HTML includes the service-worker registration call.
- `scripts/check_production_pwa.py --origin https://www.homeofferflow.com` passed.
- A cache-busted browser load no longer emitted the service-worker CSP warning.
- Vercel runtime-errors query returned no runtime errors.

The remaining browser-console connection warning is generated by a browser
extension and is unrelated to HomeOfferFlow application code.

## Result

**Pass.** Same-origin service-worker registration is now permitted in the live
production CSP, restoring the intended PWA offline shell registration path.
