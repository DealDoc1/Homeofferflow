# Temporary-lease special-provision completeness - September 18, 2026

## Defect and local correction

Both TREC 16-7 Buyer and TREC 15-7 Seller Temporary Residential Lease overlays
used `fitted_blank_entries` for Paragraph 11. On overflow, that helper replaces
the end of the entered terms with an ellipsis. A single very long unbroken
token could also yield no entries. The full-text review did not prevent these
losses in the generated signing packet.

With the same 60-term synthetic input, the old fitting helper omits the final
term and ends in `...` for both forms. The new Paragraph 11 helper retains
short terms in the existing eight-point blanks, or places a continuation
reference on the lease and reproduces the whole text on labeled pages. It
does not summarize the text, draft new terms, or alter the source PDF.

Continuation headings identify Buyer or Seller Temporary Residential Lease,
Paragraph 11, and the property. Appended pages follow the repair and non-realty
continuations; existing source-form signatures and uploaded-document offsets
are preserved. Required buyer initials are added to new pages, with seller
initials only for sellers already included by the existing execution plan.
No new recipient is inferred or invited.

## Verification

- Eight new tests exercise both lease types, 100-term multi-page examples,
  final-term retention, 800-character unbroken tokens, literal markup, empty
  and deselected leases, aliases, stale answers for the other lease, original
  signature-map stability, existing sellers, and combined repair/inventory/
  lease/upload ordering and unique field IDs.
- The initial test fixtures omitted the seller-lease selection flag required
  by the existing production adapter. Fixtures were corrected to the actual
  interview payload shape, including seller contact data. Production
  validation was not weakened to make the tests pass.
- All 30 focused continuation and source-sync tests pass.
- Full discovery: 2,071 tests in 27.874 seconds; 2,069 pass and two fail. The
  failures remain `test_every_current_map_matches_the_source_calibrated_baseline`
  and `test_current_released_maps_match_the_approved_baseline`, reflecting the
  separate, unapproved TXR-1507 candidate. Do not describe this as all-green CI.
- Four fake-data, unsigned local specimens generated by
  `scripts/qa/lease_terms_continuation_preview.py`; ten relevant rendered pages
  visually inspected using the PDF skill workflow. The short terms and
  references fit their printed blanks. Two-page continuations preserve every
  example term, repeated identification, and clear initials areas.
- `git diff --check` passes. No completed-provider initials were inspected.

## Limits and next work

Local only; no push, Vercel build/preview/deployment, email, SignWell request,
or production database write. No customer executed document was modified.
Record in the next daily report as local implementation and visual QA, not
deployed functionality or a measured conversion/revenue improvement.

This corrects lease Paragraph 11 only. Other uses of the older fitting helper
(for example utilities, pets, names and notice addresses) remain separate
overflow-review work. Purchase-contract special provisions and broker
disclosures still use their own line-limited rendering. Do not claim all
free-text fields are now lossless. The shared Helvetica font also retains its
existing character-set limitations; comprehensive Unicode support is unverified.

Fresh completed SignWell testing remains necessary for new continuation
initials. Existing signatures retain their coordinates and do not become
newly provider-verified merely because the page-offset tests pass.

## Utilities and pets follow-up - September 18

The remaining Paragraph 6 utilities and Paragraph 8 pet answers used the same
ellipsis-producing helper. With a 12-line synthetic answer, the prior helper
omits the final answer and inserts `...` for both fields on both lease forms.
These four field paths now use the complete-or-continuation layout as well.

Each short answer remains on its own source blank. Only overflowing answers
are copied, in full, into a shared lease Terms Continuation, labeled by their
original paragraph (6, 8, or 11). The narrow utilities/pets blanks use
"See continuation."; the larger special-provisions blank uses
"See attached temporary lease terms continuation." The continuation identifies
the lease type and property on every page. No new term or obligation is drafted.

Six additional tests cover short answers, individual field overflow, combined
sections and page-by-page signer assignments, supported aliases, very long
unbroken tokens and literal markup, and deselected/other-lease answer isolation.
The original mixed repair/inventory/lease/upload test now also overflows both
utilities and pets while verifying all original signatures and final upload
offsets. Production selection and recipient validation remain unchanged.

- All 36 focused continuation/source-sync tests pass.
- Full discovery: 2,077 tests in 30.971 seconds; 2,075 pass and the same two
  TXR-1507 approved-map reference tests fail. References remain unchanged.
- Generated two unsigned fake-data specimens with
  `scripts/qa/lease_answers_preview.py` and visually inspected five relevant
  rendered pages: both source-page references, two buyer continuation pages,
  and one seller continuation page. The seller example retains its complete
  special provisions on the original twelve blanks and appends only utilities
  and pets; the buyer example carries all three sections. All inspected text
  clears printed labels and initials areas. No SignWell-completed QA is claimed.
- `git diff --check` passes. No push, deployment, email, provider API call,
  or production mutation occurred.

This supersedes only the utilities/pets portion of the remaining-work note
above. Names, notice addresses, purchase-contract special provisions, broker
disclosures, and comprehensive Unicode support still need separate review.
Include this follow-up in the next daily report as local work only.
